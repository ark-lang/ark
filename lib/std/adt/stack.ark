#use std::mem

pub type Stack struct <T> {
	data: ^mut List<T>,
};

pub func (Stack) new<T>() -> ^mut Stack<T> {
	stack := mem::alloc<Stack<T>>();
	@stack = Stack<T> {
		data: List::new<T>(), 
	};
	return stack;
}