#use mem
#use io

pub func main() -> int {
	myFilePath : ^mut io::Path = (^mut io::Path)(io::Path::new("/home/felix/index.html"));
	myFile : ^mut io::File = io::File::open(myFilePath, io::FileMode::Read);

	myFile.close();

	return 0;
}