fn printf(str format, _): int;

fn println(str format, _): int {
	mut va_list arg;
	va_start(arg, format);
	printf(format, arg);
	va_end(arg);
	printf("\n");
	return true;
}

fn main(): int {
	println("this is a test!");
	return 0;
}