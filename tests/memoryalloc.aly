use "stdio"

// this is an idea
// more than a test.
// take this document
// with a grain of salt
// 
// and dont try compile
// it, it wont work!

fn main(): int {

	/*
		An array can be represented as an array,
		the index is the address, and it contains
		a value
	*/
	
	// tidle represents block of memory, not an array
	// allocates 
	test: ^i32 = ~[32];	// new block in memory

	// note the tilde in the definition
	test: ~[4: ^i32] = ~[0, 1, 2, 3];



}