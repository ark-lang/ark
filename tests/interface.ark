#use std::io

type IDoer interface {
    func do(),
};

type Doer struct {};

func (v: Doer) do() {

}

func runDoer<T: IDoer>(t: T) {
    //t.do();
    // uncomment for inferrence error
}

pub func main() -> int {
    io::println("interface test");

    return 0;
}
