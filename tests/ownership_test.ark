[c] func printf(fmt: str, ...): int;

func do_stuff(mut x: int) {
    x = 21;
}

func main(): int {
    // lifetime
    // list of owned values

    mut x: int = 10; // new owned value
    do_stuff(x); // new owned value points to previous owner, previous owner ownership = false
    C::printf("mate you're a cunt %d\n", x); // if !ownership -> use of moved value

    return 0;
}
