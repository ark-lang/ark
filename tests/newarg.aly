struct Test {
	x: int;
	t: usize;
	z: i32;
}

fn printf(fmt: str, _): int;

fn add(a: int, b: int): int {
	return a + b;
}

fn main(): int {
	int a = 5;
	int b = 10;
	printf("%d + %d = %d", a, b, add(a, b));
	return 0;
}