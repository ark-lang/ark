[c] func printf(fmt: str, ...);

struct Test2 {
    x: int,
}

struct Test {
    x: int = 1,
    y: int,
    z: f64,
    a: f32,
    e: str,
    test2: Test2,
}

func main(): int {
    mut thing: Test;

    thing.y = 9;
    printf("thing.y: %d\n", thing.y);

    thing.test2.x = 11;
    printf("thing.test2.x: %d\n", thing.test2.x);

    return 0;
}
