
// c bindings
fn printf(format: str, _): int;

// single line functions
fn add(a: int, b: int): int -> return a + b;

fn main(): int {
	x := "type inference";
	printf("%s\n", x);

	y := add(5, 5);
	printf("5 + 5 = %d\n", y);

	return 0;
}