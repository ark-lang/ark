[c] func printf(fmt: str, ...): int;

func main(): int {
    mut x := 2;
    mut y := 2;
    for x == 2 {
        y = x + y;
        if y > 100 {
            printf("%d\n", y);
            break;
        }
    }
    return 0;
}