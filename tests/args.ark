[c] func printf(fmt: ^u8, ...);

pub func main(argc: int, argv: ^^u8) -> int {
    C::printf(c"%d\n", argc);

    mut ptr := uintptr(argv);

    mut i := 0;
    for i < argc {
        str := (^^u8)(ptr);
        C::printf(c"%s\n", ^str);

        ptr += 8;
        i += 1;
    }

    return 0;
}
