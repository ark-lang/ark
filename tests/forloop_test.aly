func printf(format: str, _): int;
func getchar(): char;

func main(): int {
	mut x: int = 0;
	
	for x < 5, x = x + 1 {
		printf("%d\n", x);
	}

	// this wont work on unit test
	// since it expects input! woops...
	// mut c: char = getchar();
	// for c != -1, c = getchar() {
	// 	printf("%c", c);
	// 	if c == '!' {
	// 		printf("\n");
	// 		return 0;
	// 	}
	// }
}
