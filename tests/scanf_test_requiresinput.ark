[c] func scanf(fmt: str, ...): int;
[c] func printf(fmt: str, ...): int;

func main(): int {
    mut x: s32 = 0;

    C::printf("Input a number: ");
    C::scanf("%d", &x);
    C::printf("Your number:    %d\n", x);

    return 0;
}
