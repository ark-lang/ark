[c] func scanf(fmt: ^u8, ...) -> int;
[c] func printf(fmt: ^u8, ...) -> int;

func main() -> int {
    mut x: s32 = 0;

    C::printf("Input a number: ");
    C::scanf("%d", &x);
    C::printf("Your number:    %d\n", x);

    return 0;
}
