#use std::io
#use std::ds

pub func main() -> int {
    io::println("list test");

    list := ds::List::newWithCapacity<int>(0);
    list.append(5);

    io::printInt(list.get(1));
    io::println("");

    return 0;
}
