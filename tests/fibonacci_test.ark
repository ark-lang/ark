func fib(n: int): int {
    if n < 2 {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

[c] func printf(fmt: str, ...);

func main(): int {
    printf("fib(12) == %d\n", fib(12));

    return 0;
}
