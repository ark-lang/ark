[c] func printf(fmt: ^u8, ...) -> int;

func main() -> int {
    mut x: int = 5;

    {
        mut x: f32 = 4.321;

        {
            mut x: ^u8 = "foo";
            C::printf("x = %s\n", x);
        }

        C::printf("x = %f\n", x);
    }

    C::printf("x = %d\n", x);

    return 0;
}
