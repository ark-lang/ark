fn printf(format: str, _): int;

fn main(): int {
    y := 5;
    printf("y is %d\n", y);

    z := 21 + 5;
    printf("z is %d\n", z);

    d := 1 + 2 + 3 + 4 + 5 + 6 + 7 + 8 + 9;
    printf("d is %d\n", d);

    return 0;
}