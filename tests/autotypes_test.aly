fn printf(format: str, _): int;

fn main(): int {
    y := 5;
    printf("y is %d\n", y);

    z := 21 + 5;
    printf("z is %d\n", z);

    d := 1 + 2 + 3 + 4 + 5 + 6 + 7 + 8 + 9 + 2;
    printf("d is %d\n", d);

    b := y + z;
    printf("y + z = %d\n", b);

    terry := "Terry is the name, leet coding is the game";
    printf("%s\n", terry);

    tree := 2.0f;
    printf("%f\n", tree);

    return 0;
}