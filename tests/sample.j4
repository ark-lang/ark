#this is what im hoping j4 will look like

int y = 5;
int a, b, c;
str amazing = "fuck";

enum {
	IDENTIFIER,
	WHATEVER,
	HOMESHAKE
} whatever;

fn function_name(int x) -> int {
	return 5;
}

fn function_abc(str whatever) -> void {
	int z = 5;
	int d = z * z;
	int c = function_name(5);
	y = z + d * c;

	match {
		x {

		},
		y {

		},
		_ {
			# the underscore means
			# any value, like default: in a switch
		}
	}

	if (y > 52) {
		y = 5;
	}
	
	while (x == 32) {
		y = 10;
	}
}

fn fuck_the_police(int x): bool => x == 52;

fn main(int argc, string argv[]): int {
	int b = 123;
	function_abc("amazing");

	return EXIT_SUCCESS;
}

struct {
	int x;
	int y;
	int a;
} wow;