[c] func printf(fmt: str, ...) -> int; // A

func main() -> int { // B
    x: int = 10;
    { // C
        z: int = 30;
        e: &int = &z;
        { // D
            b: int = 32;
        }
    }
    y: &int = &x;

    return 0;
}
