[c] func printf(fmt: str, ...) -> int; // A

func move_something(a: int) {

}

func main() -> int { // B
    x: int = 10;
    move_something(x);
    move_something(x);

    { // C
        z: int = 30;
        [unused] e: &int = &z;
        { // D
            [unused] b: int = 32;
        }
    }
    [unused] y: &int = &x;

    return 0;
}
