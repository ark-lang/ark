struct Cat {
	cat_age: int,
	cat_name: str
}

// printf binding for println
func printf(format: str, _): int;

impl Cat {
	func swag(fuck: int);

	func say(say: str) {
		printf("this is temporary for now but eventually will do something with struct values\n");
	}
}

func main(): int {
	cat: Cat;
}