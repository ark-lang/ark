func add(x: ^int): ^int {
	mut d := 10;
	return &d;
}

func malloc(x: usize): ^void;
func free(x: ^void): void;
func printf(format: str, _): int;

func main(): int {
	x: ^int = malloc(8);
	printf("%d\n", add(x));
	free(x);
}