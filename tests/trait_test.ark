[c] func printf(fmt: str, ...);

trait ToString {
    func to_string(): str;
}

struct Foo {
    x: int,
}

func main(): int {
    mut thing: Foo;
    thing.x = 42;

    printf("thing.x: %d\n", thing.x);

    return 0;
}
