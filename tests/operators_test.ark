mut x: int;

func main(): int {
    x = 0;
    x = x + 5;
    x = x - 4;
    x = x * 2;
    x = x / 4;
    x = x % 4;
    x = x << 4;
    x = x >> 1;
    x = x | 127;
    x = x & 4;
    x = x ^ 2;
    x = -x * -x;
    x = x + !x;
    x = x - (x > 2);
    x = x + (x < 2);
    return x;
}