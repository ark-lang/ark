[c] func printf(fmt: str, ...): int;

enum Option {
    None,
    Some(^void)
}

func main(): int {
    [unused] x := Option::None;
    [unused] y := Option::Some(cast(^void, 0xdeadbeef));

    C::printf("None-tag: %d\n", x);
    C::printf("Some-tag: %d\n", y);
    return 0;
}