[c] func printf(fmt: str, ...): int;

enum Option {
    None,
    Some(^u8)
}

func main(): int {
    x := Option::None;
    y := Option::Some(^u8(0xdeadbeef));

    C::printf("None-tag: %d\n", x);
    C::printf("Some-tag: %d\n", y);
    return 0;
}
