fn printf(fmt: str, _): int;

fn main(): int {
	x: int = 2;
	
	if x != 2 {
		return 1;
	} else if x == 3 {
		return 2;
	} else if x == 4 {
		return 3;
	} else if x == 2 {
		return 0;
	}

    return 5;
}