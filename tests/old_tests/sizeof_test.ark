func printf(fmt: str, _);

struct Test {
	hello: int,
	world: int
}

func main(): int {
	// TODO improve this test..
	x: int = sizeof Test;
	
	if x <= 0 {
		return 1;
	}
	
	printf("%d\n", sizeof i32);
	
	return 0;
}
