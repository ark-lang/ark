// using the shorthands can get pretty messy
// i'd suggest using them sparely, or styling
// them like so
fn add(int a, int b): int 
	-> while true 
		-> if (a > b) 
			-> return (a + b)

// this isn't so bad either
fn add(int a, int b): int 
	-> while true 
	-> if (a > b) 
	-> return (a + b)

fn main(): int {
	if (a >= b) -> return 1

	int result = 0
	for a:(0, 10) -> result = (result + 1)
}